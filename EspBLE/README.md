Python file requires the use of bleak
```javascript Python
console.log('pip install bleak');
```
Requires the user to input the location of the esp32 anchors (in meters) in order for it to work properly
