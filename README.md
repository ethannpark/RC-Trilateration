Things to do:
  - Download python3 and the opencv library: (sudo apt update
                                              sudo apt install python3-pip python3-venv
                                              pip3 install flask opencv-python)
  - Set up script running "python app.py" when raspberry pi boots
